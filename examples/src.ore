def p(msg string) void {
	print_string_length(msg, strlen(msg))
}

def p(num int) void {
	print(num)
}

def p(ptr *int) void {
	print(*ptr)
}

def main() int {
	let v = 500
	p('gochiusa')
	p(1)
	p(&v)
	0
}
